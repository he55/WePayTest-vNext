@page
@model WePayServer.Pages.Order.DeleteModel

@{
    ViewData["Title"] = "删除";
}

<h1>是否确认删除？</h1>
<hr />

<div>
    <dl class="row">
        <dt class="col-sm-2">
            订单号
        </dt>
        <dd class="col-sm-10">
            @Model.WePayOrder.OrderId
        </dd>
        <dt class="col-sm-2">
            订单金额
        </dt>
        <dd class="col-sm-10">
            @Model.WePayOrder.OrderAmount.ToString("F2")
        </dd>
        <dt class="col-sm-2">
            Code
        </dt>
        <dd class="col-sm-10">
            @Model.WePayOrder.OrderCode
        </dd>
        <dt class="col-sm-2">
            支付状态
        </dt>
        <dd class="col-sm-10">
            @if (Model.WePayOrder.OrderIsPay)
            {
                <span style="color:green;">已支付</span>
            }
            else
            {
                <span>未支付</span>
            }
        </dd>
        <dt class="col-sm-2">
            创建时间
        </dt>
        <dd class="col-sm-10">
            @DateTimeOffset.FromUnixTimeMilliseconds(Model.WePayOrder.CreateTime).LocalDateTime
        </dd>
        <dt class="col-sm-2">
            更新时间
        </dt>
        <dd class="col-sm-10">
            @DateTimeOffset.FromUnixTimeMilliseconds(Model.WePayOrder.UpdateTime).LocalDateTime
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="WePayOrder.Id" />
        <input type="submit" value="删除" class="btn btn-danger" /> |
        <a asp-page="./Index">返回列表</a>
    </form>
</div>
